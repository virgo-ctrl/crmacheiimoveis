# supabase

- `migrations/` — geradas a partir de `schema/schema.dbml`.
- `policies/` — RLS (row-level security) por escopo self/team/all. Ver `docs/01-acessos-rbac.md`.
- `seed/` — papéis, etapas de funil, origens e motivos de perda padrão.
