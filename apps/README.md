# apps

- `web/` — Next.js: UI + BFF (todos os módulos de tela) e webhooks de entrada.
- `workers/` — processamento assíncrono: consumers de canal, filas e jobs agendados (D1/D2/D3/D4).

Ver `docs/09-estrutura-de-pastas.md`.
