# packages

Código compartilhado. `core/` é o domínio e NÃO importa framework nem banco concreto; os demais pacotes implementam interfaces do core (adapters) ou são utilitários compartilhados.

Ver `docs/09-estrutura-de-pastas.md` › Regras de dependência.
